<?xml version="1.0"?>
<project name="ROOT" default="package" basedir="../../ROOT">
    <property name="app.name" value="ROOT"/>
    <property name="classpath.dir" value="/opt/tomcat/lib"/>
    <property name="deploy.dir" value="/home/doug/Sites/localcity/"/>
    
    <target name="clean">
        <delete file="${deploy.dir}/${app.name}.war"/>
    </target>

    <target name="package" depends="clean">
        <jar destfile="${deploy.dir}/${app.name}.war" basedir="${basedir}"/>
    </target>
</project>
